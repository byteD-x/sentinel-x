# 安全政策

## 支持范围

Sentinel-X 当前处于开发前设计阶段，没有发布版本或生产支持承诺。未来只支持文档明确列出的本地隔离演练版本；真实生产系统接入、任意 Shell、数据库修复和集群级操作不在支持范围内。

## 报告安全问题

不要在公开 Issue、演示录屏或事故包中提交密钥、Cookie、ServiceAccount token、审批凭证、真实生产日志或可复用攻击载荷。

发现问题时：

1. 停止相关演练并启用 kill switch；不要继续尝试扩大影响。
2. 记录受影响版本/commit、环境 profile、复现条件、期望/实际结果和脱敏证据。
3. 通过代码托管平台为仓库所有者配置的私有安全报告通道联系维护者；仓库尚未配置该通道时，先直接联系所有者，不公开细节。
4. 保留原始证据 hash，但移除秘密值和个人信息。

本文不提供虚构邮箱或响应时限。建立公开仓库前，发布门禁要求配置真实私有报告渠道和责任人。

## 需要优先报告的问题

- 未审批、过期、撤销或篡改计划仍能执行。
- R2/R3、跨 namespace、Secrets、`pods/exec` 或任意命令可达。
- 相同幂等键造成重复副作用。
- 提示注入改变工具、策略、审批或外发边界。
- Action Gateway 接受伪造 Worker 身份或不读取权威审批。
- ground truth 泄露给 Investigator。
- 日志、Trace、导出包或错误响应泄露凭据。
- Scenario cleanup 影响控制面或真实系统。

## 安全处理原则

- 默认拒绝并保持最小权限。
- 修复前先添加可重复的失败测试，修复后运行目标和安全回归。
- 不删除审计证据；必要修正通过追加事件完成。
- 对已暴露秘密立即轮换，不能只从 Git 当前版本删除。
- 对动作最终状态未知的情况先协调和人工核实，禁止盲目重试。

详细威胁和控制见 [安全模型](docs/security-model.md)，开发者规则见 [AGENTS.md](AGENTS.md)。
