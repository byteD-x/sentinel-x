[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sentinel-x-domain"
version = "0.1.0"
description = "Sentinel-X domain logic and state machine"
requires-python = ">=3.13"
dependencies = ["sentinel-x-contracts>=0.1"]
